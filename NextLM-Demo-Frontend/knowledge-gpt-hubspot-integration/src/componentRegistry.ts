/**
 * Component registry for the Knowledge GPT HubSpot Integration.
 * Currently not using external components - using direct HTTP client approach.
 */

import { componentManifests } from "@prismatic-io/spectral";

export const componentRegistry = componentManifests({
  // No external components needed - using direct HTTP client
});
